---
kind: Service
apiVersion: v1
metadata:
  name: mcp-server-aap
  namespace: mcp
  labels:
    app: mcp-server-aap
spec:
  type: NodePort
  selector:
    app: mcp-server-aap
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080