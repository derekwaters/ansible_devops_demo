---
aap_organizations:
  - name: "{{ org_platform.name }}"
    description: "{{ org_platform.description }}"
    state: present
      
  - name: "{{ org_app.name }}"
    description: "{{ org_app.description }}"
    state: present
...